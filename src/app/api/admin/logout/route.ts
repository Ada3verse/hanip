import { NextResponse } from "next/server";import { isSameOriginRequest } from "@/lib/security/http";import { ADMIN_SESSION_COOKIE } from "@/lib/admin/adminAuth";import { getAdminSecurityStore } from "@/lib/admin/adminContext";import { auditAdmin,requireAdministrator } from "@/lib/admin/adminRequest";
export async function POST(request:Request){if(!isSameOriginRequest(request))return NextResponse.json({authenticated:false},{status:403});const admin=await requireAdministrator();if(admin){await getAdminSecurityStore().revokeSession(admin.id);await auditAdmin(admin.adminId,"admin_logout","administrator",admin.adminId,"logout");}const response=NextResponse.json({authenticated:false});response.cookies.set(ADMIN_SESSION_COOKIE,"",{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"strict",path:"/",maxAge:0});return response;}
